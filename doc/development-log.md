# 开发日志

更新日期：2026-06-26

## 项目起点

项目从 `doc/requirement.md` 出发，目标是做一个个人使用的瘦身助手 Web 应用。第一版定位为自己使用，不做多人账号体系，不接自动健康数据源，只支持手动录入。

## 对话中的需求变更记录

1. 使用范围
   - 第一版只给自己用。
   - 先做 Web，默认跨平台访问。
   - 不做传统账号体系，使用访问设备识别。

2. 访问保护
   - 首次访问设备需要输入一个访问密码。
   - 已信任设备后不再重复输入密码。
   - 密码暂时保存在服务器本地数据库中，只保存哈希。
   - 后续补充了访问密码修改和受信设备移除能力。

3. 数据录入
   - 只做手动录入。
   - 健康记录每天只记录一次，多次提交覆盖当天记录。
   - 跑步记录可以同一天多条。
   - 历史页需要支持补充历史记录，选择日期后进入对应日期的录入页。

4. 目标与反馈
   - 需要设置健康目标和跑步目标。
   - 跑步目标包括每周次数、每周跑量。
   - 健康数据和运动数据同等重要。
   - 首页需要反馈距离目标还有多远、按当前趋势还需多久、给出克制的鼓励文案。
   - BMI 可以显示，身高放到配置界面维护。

5. 配置中心
   - 参数需要可配置。
   - 身高、提醒邮箱、趋势估算下限、提醒规则、SMTP、访问保护均放入配置中心。
   - 趋势估算参数需要系统下限，不能随意设得过低。

6. 提醒
   - 接受邮件提醒和站内提醒。
   - 后续补充 SMTP 配置、测试邮件、邮件提醒失败状态保留。

7. UI 与语言
   - 网站必须是中文。
   - 风格要简洁。
   - 首页不要把“今日状态”放在最显眼的大卡片位置，改为右上角紧凑状态面板。
   - 宽屏下不要留下大面积右侧空白，主内容应使用可用宽度。

8. 数据可视化
   - 首页不能全是文字，需要方便观察的曲线图。
   - 健康曲线和运动曲线不能密密麻麻堆满多张小图。
   - 每类曲线改为单张主图，通过下拉框切换指标。
   - 曲线需要纵轴。
   - 鼠标悬浮到某一天需要浮现该日数值。

9. 跑步配速
   - 平均配速不应由用户手动填写。
   - 配速应由跑步时长 / 公里数自动计算。
   - 录入页和编辑页中的配速改为只读计算项。

10. 开发辅助
    - 原 MCP 浏览器能力在当前会话不可用。
    - 增加本地 Playwright 截图脚本，用真实浏览器渲染桌面、宽屏、移动端截图，辅助后续 UI 调整。

## 当前开发进度

### 已完成

- Epic 1：应用骨架、本地数据库、访问密码、受信设备基础能力。
- Epic 2：健康记录、跑步记录、历史浏览筛选、编辑和删除。
- Epic 3：目标设置、首页摘要、趋势、BMI、目标进度、预计达成时间、鼓励文案。
- Epic 4：配置中心、个人资料、趋势阈值、提醒规则、站内提醒、SMTP、邮件提醒、访问保护管理。
- 后续对话增量：
  - 历史页补录入口。
  - 首页健康/运动曲线。
  - hydration mismatch 处理。
  - 首页状态面板位置调整。
  - 本地 Playwright 截图脚本。
  - 宽屏主内容铺满。
  - 曲线下拉切换、纵轴、悬浮提示。
  - 跑步配速只读自动计算。

### 当前质量状态

最近一次完整验证：

```text
npm test：84 个测试通过
npm run typecheck：通过
npm run lint：通过
npm run build：通过
npm run ui:screenshot：通过
```

### 当前关键能力

- 中文 Next.js Web 应用。
- 本地 SQLite + Drizzle ORM。
- 设备信任 + 本地访问密码保护。
- 健康数据手动录入与覆盖。
- 跑步数据手动录入，配速自动计算。
- 历史记录筛选、补录、编辑、删除。
- 健康目标和跑步目标。
- 首页摘要、目标进度、趋势估算、BMI、鼓励文案。
- 健康曲线和运动曲线交互式展示。
- 配置中心。
- 站内提醒和 SMTP 邮件提醒。
- 本地截图回归脚本。

### 已知注意事项

- `npm install` 后 `npm audit` 仍报告 6 个 moderate 级别问题，尚未执行 `npm audit fix --force`，避免引入破坏性依赖变更。
- `.ui-screenshots/`、`.next-dev-logs/`、`.agents/` 属于本地运行或工具产物，不纳入版本库。
- 当前是个人或小范围私有使用版本，账号体系是管理员、普通用户、访客模式的轻量实现，不是面向公网开放注册的完整认证系统。

## 2026-06-26 首页与记录体验增量调整

### 调整背景

本轮调整来自对现有页面的实际使用反馈：首页目标反馈不够突出，曲线时间范围固定且切换反馈不明显，记录页更像导航页而不是提交页，历史记录操作按钮位置影响浏览效率。

### 已完成调整

- 首页目标进度前置到首页摘要和数据曲线之前，并放大目标卡片和环形完成度图。
- 目标摘要不再只显示“已设置”，改为直接显示健康目标或跑步目标的剩余差距，并在描述中展示当前值、目标值和跑步目标差距。
- 健康曲线和运动曲线增加时间范围下拉，支持最近 7 天、最近 1 个月、最近半年、最近 1 年，默认最近 7 天。
- 修复健康曲线切换到最近 1 个月时看起来没有变化的问题：图表横轴现在使用所选时间范围的固定起止日期，而不是仅用实际数据点的起止日期。
- 移除底部重复的“趋势和 BMI”区块；趋势统一通过数据曲线查看，当前 BMI 放入健康摘要，BMI 趋势保留在健康曲线指标中。
- 记录页改为“提交记录”工作台：顶部选择记录日期，下面直接展示健康记录和跑步记录两个提交表单。
- 历史记录卡片的编辑、删除操作移动到每条记录标题行右侧，去掉底部独立操作栏。

### 验证记录

本轮相关变更已执行以下验证：

```text
node --test tests/dashboard-summary.test.mjs
node --test --test-skip-pattern "主要路由" tests/app-shell.test.mjs
npm run typecheck
npm run lint
```

完整路由渲染测试仍可能受本地已有 Next dev server 影响；本轮未强制终止现有开发服务。

## 2026-07-08 账户、邮件提醒、自动部署和首页体验固化

### 调整背景

本轮调整来自云端部署和真实使用反馈：需要最简账号隔离、管理员/普通用户/访客模式、普通用户收件邮箱、邮件提醒可靠触发、宝塔自动化部署、登录页和首页视觉动效，以及首页目标卡片结构重新梳理。

### 已完成调整

- 账号体系改为轻量实现：管理员可在设置中新增、修改、删除普通用户；普通用户只访问自己的个人数据；访客模式只做临时体验，不持久化数据。
- 默认管理员账号由环境或初始化逻辑确定，早期测试环境可使用 `admin / admin`，生产部署后应尽快修改密码，避免浏览器和密码管理器提示弱口令或泄露风险。
- 管理员创建用户时填写的显示名称作为该用户默认昵称；普通用户进入设置页时应显示这个昵称，并允许自行修改。
- SMTP 发信服务器由管理员统一维护；普通用户只设置自己的提醒收件邮箱，并提供测试邮件按钮。
- 邮件提醒后台任务由生产启动脚本拉起，每分钟检查提醒规则；提醒事件按提醒时间区分，用户当天调整提醒时间或收件邮箱后允许重新触发，不再用“同一用户同一天已有任意邮件事件”全局阻断。
- 每日提醒时间改为小时和分钟两个下拉框，分钟支持 `0-59`，保存时仍转换为 `HH:mm`。
- 登录页和首次创建管理员页增加全屏运动背景、鼠标移动反馈和点击反馈；登录后的应用背景铺满整个视口，并尊重 `prefers-reduced-motion`。
- 首页目标区由“目标体重 / 目标腰围”两张卡片调整为“健康目标 / 运动目标”两张卡片：健康目标承载体重和腰围，运动目标承载每周跑步次数和每周跑量。
- 首页状态配色固化：未设置目标使用蓝灰中性提示；健康目标使用翠绿色系；运动目标使用运动蓝色系；今日打卡未完成使用红色，完成使用翠绿色。
- 自动化云端部署脚本落地到项目内，发布包上传到 `/www/wwwroot`，依赖安装、数据库迁移、`better-sqlite3` alias 修复、旧发布包清理和宝塔 Node 项目修复由脚本处理；服务器凭据不写入仓库，通过环境变量或执行时补齐。

### 防回归约定

- 后续 UI 调整不得把首页第一屏目标区改回单独的“目标体重 / 目标腰围”卡片。
- 后续提醒逻辑不得恢复为“同一用户同一天只允许一次邮件提醒”的粗粒度拦截。
- 后续普通用户设置页必须保留收件邮箱和测试邮件能力，SMTP 配置只对管理员开放。
- 后续访客模式不得写入持久数据库，也不得开放设置页。
- 后续部署改动不得把 `npm install`、`npm run build` 或 `npm run prepare:bt` 放进宝塔启动命令；启动命令只负责启动长驻服务。
- 后续涉及登录页、首页或全局视觉的变更，需要生成并检查桌面、宽屏和移动端截图。
- 后续登录页或首页视觉明显变化时，需要同步更新 README 的界面预览截图。
- 登录后的运动背景和鼠标跟随效果属于 AppShell 全局能力，不能只在首页单独挂载。

### 验证记录

本轮相关变更执行过以下验证：

```text
npm run lint
npm run typecheck
npm test
npm run ui:screenshot
云端部署脚本执行成功，生产健康检查通过
```

## 2026-07-09 打卡输入范围与交互优化

### 调整背景

打卡页原先存在两个问题：健康数据使用 0.1 粒度下拉框，体重这类字段会生成上千个选项；跑步数据使用自由文本输入，心率、步频、步幅等指标缺少常规范围约束，容易录入脏数据。

### 已完成调整

- 健康打卡和健康记录编辑改为数值输入，保留单位栏，并显示常规人体范围提示。
- 跑步打卡和跑步记录编辑改为数值输入，距离、时长、心率、步幅、步频分别设置合理的 `min` / `max` / `step`。
- 时长、平均心率、步频要求整数；步幅支持 0.01 米；体重、围度、体脂率和跑步距离支持小数步进。
- 配速继续只读自动计算，并在后端拒绝明显不合理的公里数和时长组合。
- 前端表单和后端解析共用 `features/records/constants/record-input-rules.ts`，避免 UI 范围和服务端校验分叉。

### 防回归约定

- 不要把体重、腰围、臀围、体脂率改回 0.1 粒度的超长下拉框。
- 不要把跑步距离、时长、心率、步幅、步频改回自由文本输入。
- 修改打卡字段范围时必须同步更新共享规则、后端测试和 UI 截图。

### 验证记录

```text
npm run lint
npm run typecheck
npm test
npm run ui:screenshot
```

## 2026-07-15 个人站切换、Go/Astro 迁移与遗留文件清理

### 调整背景

本轮调整来自站点定位变化和后续部署维护要求：根路径需要从“跑步瘦身助手”改为个人 Web 站点首页，跑步瘦身助手降级为项目板块中的独立应用；后端运行时从 Node/Next Server Actions 迁移到 Go API，方便后续在云服务器和宝塔环境中部署、监控和维护。

### 已完成调整

- 根首页切换为个人站结构，包含认知、技术、项目三个板块；跑步瘦身助手作为项目入口保留。
- 前端切换为 Astro + Vue，跑步瘦身助手入口位于 `/app/slimming`，项目详情位于 `/projects/slimming`。
- 后端切换为 Go API + SQLite，覆盖登录、用户管理、健康记录、跑步记录、目标、历史、设置、提醒和邮件配置。
- 默认开发、构建、启动、发布脚本切换到 Go/Astro 链路：`dev-go-astro`、`start-go-astro`、`create-go-astro-release-package`。
- 宝塔发布包改为 Go 二进制 + Astro 静态站点，生产入口由 Go 服务托管静态文件并提供 `/api/*`。
- 跑步瘦身助手恢复旧版导航行为：默认进入首页，点击首页、打卡、数据、历史、设置时只显示对应分区。
- 登录页恢复独立全屏运动背景和居中登录卡片；登录后恢复应用动态背景、首页可交互卡片、左右翻页热区和统一按钮视觉。
- 默认管理员由 Go 启动时保证存在，当前默认账号为 `admin / admin123456`。
- 清理重构前遗留的旧 Next/React/Drizzle 源码、shadcn 配置、根 `public/` 静态目录、旧 Next 构建产物、旧截图产物和临时旧 CSS 导出文件。
- `.gitignore` 增加 `.dev-logs/` 与 `server/data/`，避免开发日志和本地 SQLite 数据库进入版本库。

### 防回归约定

- 不要恢复 Next.js App Router、React Server Actions、Drizzle、根 `public/` 或 Node 后端启动链路。
- 不要把根路径 `/` 改回跑步瘦身助手；跑步助手应用入口保持 `/app/slimming`。
- 跑步助手页面必须保持分区隔离，不能把首页、打卡、数据、历史、设置的全部卡片一次性展示。
- 登录页和应用页的运动背景、首页卡片交互、翻页热区和统一按钮视觉属于核心体验，不应在后续样式调整中移除。
- 本地运行数据只放在 `server/data/`，发布产物只放在 `dist/` 或发布包内，不进入版本库。

### 验证记录

```text
npm run check
GET http://127.0.0.1:4321/app/slimming/ => 200
POST http://127.0.0.1:8080/api/auth/login admin/admin123456 => authenticated=true
```

## 2026-07-15 个人站首页与项目页视觉重设计

### 调整背景

当前个人站已经完成从跑步瘦身助手首页到个人 Web 站点的架构切换，但首页视觉仍偏工具化，缺少个人站应有的信息层级、视觉记忆点和内容分区节奏。本轮参考 NN/g、Material Design 3、Apple HIG 与 WebAIM 的通用设计原则，重新整理首页和项目详情页的视觉表达。

### 已完成调整

- 首页首屏改为沉浸式视觉结构，使用跑步瘦身助手预览图作为低透明度背景，突出 `Hangge` 作为第一视觉信号。
- 首页补充认知、技术、项目三块结构入口，降低用户理解站点结构的成本。
- 认知卡片增加类型标签，技术板块增加序号和更明确的技术描述。
- 项目卡片改为图像、说明和箭头入口的横向布局，跑步瘦身助手继续作为项目板块 item。
- 项目详情页改为全屏图像背景 + 文案说明，保留“进入应用”和“返回个人站”两个核心动作。
- 全局个人站视觉变量调整为纸色、墨色、绿色、暖金和陶土色的组合，减少单一蓝色视觉。
- 统一个人站卡片、按钮、阴影、留白、移动端断点和 hover 反馈。
- 防回归测试从旧 `detail-preview` 项目预览结构更新为新的 `detail-backdrop` 全屏背景结构。

### 防回归约定

- 根首页仍然是个人 Web 站点，不应回退为跑步瘦身助手功能首页。
- 跑步瘦身助手只能作为项目板块入口和独立应用入口出现，不能重新抢占个人站首屏定位。
- 首页必须保持认知、技术、项目三个板块的清晰分区。
- 首屏文案、按钮和背景图需要保持足够对比度；后续换图时必须检查移动端可读性。
- 个人站视觉调整不能影响 `/app/slimming` 内部跑步助手的功能逻辑。

### 验证记录

```text
npm run check
npm run build
Chrome 桌面端截图检查：首页与项目详情页首屏布局正常
Chrome 移动端截图检查：首页首屏和认知板块布局正常
```
